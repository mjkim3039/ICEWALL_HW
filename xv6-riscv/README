# xv6-riscv with CFS Scheduler

This project extends **xv6-riscv** with a simple implementation of the **Completely Fair Scheduler (CFS)**.

## Features
- Weighted fair scheduling (nice → weight)
- Per-process `vruntime` accounting
- Min-heap based runqueue
- Preemption integrated with `usertrap`
